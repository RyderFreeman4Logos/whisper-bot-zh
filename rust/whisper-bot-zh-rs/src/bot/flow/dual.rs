//! TODO: dual-model parallel refinement with progressive Telegram edits.
//! This is the largest expected file (~150 lines target). Mirror
//! `_run_dual_refinement` + `_render_dual_progress` in the Python tree.
